<h1>CloudEvents</h1>
<% if user_signed_in? %>
    <%= current_user.email %>
    <%= link_to('Cerrar sesión', destroy_user_session_path, method: :delete)%>
<% end %>
<%= react_component 'Events', events: @events, categories: @categories, types: @types %>
